/// <reference types="react" />
import { CalendarProps } from "../../helpers/types";
declare function Calendar({ date, onPrev, onNext, fromDate, toDate, renderCell, }: CalendarProps): JSX.Element;
export default Calendar;
